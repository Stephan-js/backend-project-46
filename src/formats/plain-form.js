import _ from 'lodash';
import getSpaces from '../getSpaces.js';

const genDiffP = (diff, deep = 1) => {
  const res = [];
  const spaces = getSpaces(deep);

  return res;
};

export default genDiffP;
